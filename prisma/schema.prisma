generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Lecture {
  link        String   @id
  title       String
  image       String
  channelName String
  publishedAt DateTime
}
